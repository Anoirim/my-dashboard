# 나의 대시보드 프로젝트 — 인수인계 정리

새 대화에서 이 문서를 붙여넣으면 이어서 작업할 수 있습니다.

## 1. 개요
개인용 웹 대시보드. 단일 `index.html`(HTML/CSS/JS, Chart.js CDN) + Vercel 서버리스 프록시(`api/kis.js`)로 구성. 서버·DB 없이 브라우저 저장소(localStorage) 사용.

## 2. 위치 / 배포
- **작업 폴더(수정은 여기서만)**: `D:\Work\Mine\4_Coding\Web\my-dashboard`
- **GitHub**: https://github.com/Anoirim/my-dashboard (main 브랜치, Public)
- **실제 사용 주소(Vercel)**: https://my-dashboard-gules-six-28.vercel.app
- **배포 방식**: 파일 수정 → git commit & push → **Vercel 자동 배포**(1~2분) → 브라우저 Ctrl+Shift+R
- (구) GitHub Pages `anoirim.github.io/my-dashboard` = 프록시 없어 국내주식·잔고 안 됨. **사용 안 함.**

## 3. Vercel 환경변수
- `KIS_APPKEY`, `KIS_APPSECRET` — 시세 조회용(아무 계좌 키나 가능)
- `KIS_ACCT1_NAME`=ISA, `KIS_ACCT1_NO`(10자리), `KIS_ACCT1_APPKEY`, `KIS_ACCT1_APPSECRET`
- `KIS_ACCT2_NAME`=일반, `KIS_ACCT2_NO`, `KIS_ACCT2_APPKEY`, `KIS_ACCT2_APPSECRET`
  - **중요: KIS는 계좌마다 API 키가 따로.** 계좌별로 각자 키 필요(최대 4계좌: ACCT1~4).
- `KV_REST_API_URL`, `KV_REST_API_TOKEN` 등 — Vercel KV(Upstash) 연결됨. **KIS 토큰 캐시**(앱키별, 하루 1회 발급).
- `KIS_ENV`=`mock` (설정 시 모의투자 서버. 현재는 실전).

## 4. 기능 목록
1. **타이머** — 프리셋 2/10/15분, 시·분·초 커스텀, 종료 시 알람음(Web Audio).
2. **스케줄러** — 시간+할 일, localStorage 저장.
3. **안약 복용** — 오전/오후 각 코솝·알파간(총 4회), 날짜별 자동 초기화.
4. **본전·목표가 계산기** — 매수가+수수료+주식/ETF(거래세 0.2%/면제)+목표수익률.
5. **월 지출 관리** — 월별(YYYY-MM) 저장·조회, 카드값(결제일·계산포함 체크박스), 정기지출(결제일·결제수단), 직전달 자동이월+수동 이월 버튼. 상단: 카드값 총액 / 정기지출 합계(+카드정기·카드외정기). 하단 요약: 기타(카드 비정기)·총지출 합계.
6. **주식 분석** — 국내(KIS)/해외(Finnhub) 전환. 종목명, 현재가·등락, **일봉 추세 라인차트(약5개월)**, **실시간 5초 자동갱신**, "쉬운 해석"(주린이용, 신뢰도 배지 高/中/참고). ETF는 NAV·괴리율·순자산·거래량, 개별주는 PER·PBR·ROE·부채비율·EPS·시총·1년위치.
7. **내 보유 종목(두 계좌 통합)** — KIS 잔고 API. 계좌·종목·수량·매입가·현재가·평가금액·평가손익(률)·본전가·목표가 + 합계(총평가·총손익·수익률·목표평가금액). 수수료율·목표수익률 입력값으로 계산.
8. **백업/복원** — 상단 버튼(JSON 내보내기/가져오기). "자동 기록 파일 연결"(File System Access)은 **http(Vercel)에서만** 동작.

## 5. 프록시(`api/kis.js`) 동작
- `?symbol=005930` (기본, action=price) → 현재가+종목명+일봉+재무/ETF 전체를 **한 번에** 반환(토큰 1개 공유).
- `?action=quote&symbol=` → 실시간용 경량 현재가.
- `?action=daily|finance&symbol=` → 개별.
- `?action=balance` → 두 계좌 잔고 통합(계좌별 키·토큰, 계좌간 0.6초 딜레이 + 초당제한 시 1초 후 재시도).
- `?action=debug&symbol=` → KIS 원본 필드 진단용.
- 종목명은 현재가 API에 없어 **일별시세 output1.hts_kor_isnm**에서 가져옴.
- ETF 판별: 현재가의 PER·PBR이 0이면 ETF.

## 6. 알아둘 제약 / 주의
- 국내주식·잔고는 **Vercel 주소에서만** 됨.
- KIS **초당 호출 제한** 있음(잔고에 딜레이·재시도 적용). 실시간은 5초 간격.
- KIS 토큰은 24h 유효 → KV로 재사용(매 호출 발급 금지).
- 데이터는 브라우저 저장소 기반 → 브라우저 방문기록 삭제 시 사라질 수 있음(백업 권장).

## 7. 다음에 할 만한 것(미구현/아이디어)
- 배당수익률(분배율) 표시
- 외국인·기관 수급 지표
- 카드 사용내역 연동(개인은 CODEF 등 중개 API + 백엔드 필요)
- 구글 캘린더 연동(무료, http 필요)
- 실시간 WebSocket 시세(현재는 5초 폴링)
